<% admin_layout 'full-width' %>

<% admin_breadcrumb(link_to plural_resource_name(Spree::ProductFeed), spree.admin_product_feeds_path(@feed)) %>
<% admin_breadcrumb("#{t('spree.actions.edit')} #{Spree::ProductFeed.model_name.human}") %>

<% content_for :page_actions do %>
<% end %>

<%= render partial: 'spree/shared/error_messages', locals: { target: @product_feed } %>

<%= form_for [:admin, @product_feed], method: :put do |f| %>
  <%= render partial: 'form', locals: { f: f, action: 'edit_product_feed' } %>
  <%= render partial: 'spree/admin/shared/edit_resource_links' %>
<% end %>
